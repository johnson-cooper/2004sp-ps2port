#pragma once

// Normally CMake-generated by miniz's build (GenerateExportHeader). Client3 links miniz statically
// into one executable, so no import/export decoration is needed - this stub just satisfies the
// #include in miniz.h/miniz_zip.h/miniz_tinfl.h/miniz_tdef.h.
#define MINIZ_EXPORT
